# Repository Guidelines

## 프로젝트 구조와 모듈 구성

이 저장소는 현재 빈 Git 스캐폴드입니다. 루트에는 프로젝트 설정과 문서만 두고, 애플리케이션을 추가할 때는 다음 구조를 사용합니다.

- `src/`: 기능 또는 도메인별로 구성한 운영 소스 코드
- `tests/`: `src/` 구조를 반영하는 자동화 테스트
- `assets/`: 이미지, 샘플 데이터 등 버전 관리할 정적 파일
- `docs/`: 설계 기록, 설정 방법, 아키텍처 문서
- `.agents-tmp/`: 에이전트가 만든 재사용 가능한 임시 산출물 전용 디렉터리. 일회용 파일은 즉시 삭제합니다.

생성물, 로컬 가상환경, 비밀값, 에디터별 설정 파일은 버전 관리하지 않습니다. 도구를 도입할 때 적절한 항목을 `.gitignore`에 추가합니다.

## 빌드, 테스트, 개발 명령

빌드 및 테스트 도구는 아직 설정되지 않았습니다. 언어 또는 프레임워크를 도입할 때 표준 명령을 이 문서와 패키지 또는 빌드 설정에 기록합니다. 다음 작업에 해당하는 명령을 제공해야 합니다.

```bash
# Install project dependencies
# Run the application locally
# Run all automated tests
# Run formatting and lint checks
```

Python 작업에서는 의존성을 설치하거나 도구를 실행하기 전에 `conda activate codex` 또는 프로젝트 로컬 가상환경처럼 격리된 환경을 사용합니다.

## 코딩 스타일과 명명 규칙

선택한 기술 스택의 formatter와 linter를 따르며, 생성된 결과물을 수동으로 정렬하지 않습니다. Python은 4칸 들여쓰기를 사용하고 다른 언어는 해당 생태계의 표준을 따릅니다. 파일과 모듈은 언어 관례에 맞게 이름을 짓고, 식별자는 의미가 분명해야 하며 함수는 한 가지 역할에 집중합니다. 코드 주석은 영어로 작성합니다. 문서화되지 않은 환경 가정보다 명시적인 설정을 우선합니다.

## 테스트 지침

기능마다 테스트를 추가하고 테스트 디렉터리 구조를 운영 코드와 맞춥니다. `test_rejects_expired_token`, `UserProfile.spec.ts`처럼 기대 동작이 드러나는 이름을 사용합니다. 리뷰를 요청하기 전 전체 테스트와 관련 formatter/linter 검사를 실행합니다. 버그 수정에는 재발 방지 테스트를 포함합니다.

## 커밋과 Pull Request 지침

아직 커밋 이력이 없으므로 `Add resume parser`, `Fix invalid date validation`처럼 간결한 명령형 제목을 사용합니다. 커밋은 하나의 목적에만 집중합니다. Pull Request에는 변경 내용과 이유를 설명하고, 관련 이슈가 있으면 연결하며, 수행한 검증을 적습니다. 사용자 화면 변경에는 스크린샷을 첨부합니다. 자격 증명, 토큰, 개인정보는 커밋하지 않습니다.
